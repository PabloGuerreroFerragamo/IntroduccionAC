#include <stdio.h>

int main(){
    char frase[] = "Suelta lo Ferragamo";
    printf("Best phrase of Mora Album: %s", frase);
    return 0;
}

/*Letras después del paréntesis en C

int (d)

float (f): Número decimal. Ejemplo: altura almacena números decimales.

double (lf): Número decimal de doble precisión. Ejemplo, precio; declara una variable llamada precio que almacena números decimales con mayor precisión que float.

char (c): caracter. almacena un solo carácter.

bool (bool)

short (hd): Números enteros más pequeños que int.

long (ld): Números enteros más grandes que int.

unsigned (u): Variables sin signo (no negativas)*/
